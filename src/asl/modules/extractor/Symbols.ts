export abstract class Symbols {

    // prefixes

    static COMMENT_PREFIX = '//';
    static TAG_PREFIX = '@';

    // separators

    static TITLE_SEPARATOR = ':';
    static TABLE_CELL_SEPARATOR = '|';

}