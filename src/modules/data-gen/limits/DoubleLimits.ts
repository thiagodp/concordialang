/**
 * Limits for double values.
 * 
 * @author Thiago Delgado Pinto
 */
export abstract class DoubleLimits {	
	static MIN: number = Number.MIN_SAFE_INTEGER;	
	static MAX: number = Number.MAX_SAFE_INTEGER; 
}