import { Spec } from "../../ast/Spec";

/**
 * Query checker
 * 
 * @author Thiago Delgado Pinto
 */
export class QueryChecker {

    async check( spec: Spec ): Promise< boolean > {
        
        return new Promise< boolean >( ( resolve, reject ) => {

        } );

    }

}