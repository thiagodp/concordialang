import { NLP } from '../../modules/nlp/NLP';
import { UIElementItemLexer } from '../../modules/lexer/UIElementItemLexer';

describe( 'UIElementItemLexerTest', () => {

    /*
    let nlp = new NLP();
    let sentenceProcessor = new Sen
    let lexer = new UIElementItemLexer( sentenceProcessor ); // under test
    */

    it( 'passes', () => {
        expect( true ).toBe( true );
    } );

} );